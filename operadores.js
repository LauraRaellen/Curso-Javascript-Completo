/*
 Os operadores Js são para atribuir valores, comparar valores, executar operações aritméticas e muito mais.

 São os sinais que usamos: + - / * = ++ -- += -= =* =/ {&&(e) || (ou) ! (negação)} != !== < > <= >= etc..

 São operados em 6 "categorias"
 1) Operadores aritméticos (matemáticos)
 2) Operadores de atribuição
 3) Operadores de sequência
 4) Operadores de comparação
 5) Operadores condicional (Ternário)
 6) Operadores lógicos
*/

// let valor1, valor2, total;

// valor1 = 1;

// valor2 = 2;

// valor1 != valor2; 
//alert(valor1)


// let idade, eleitor, resultado;
// idade = 69;
// eleitor = (idade < 18) ? "Não Eleitor":"Sim, eleitor";
// alert(`A resposta é: ${eleitor}, a idade dele(a) é de ${idade}`)

// resultado = (idade > 60 && idade < 70) 
// alert(resultado)

// resultado = (idade === 65 || idade === 72)
// alert(resultado)

// resultado = !(idade === 65)
// alert(resultado)

